@using EPAM.AWARDS.ASPPL.Models
@{
    Layout = null;
    if (IsPost)
    {
        string title = Request["title"];       
        BaseModels.bll.CreateAward(title);
    }
    Response.Redirect("~/Index.cshtml");
}